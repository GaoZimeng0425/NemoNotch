{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; if echo \"$f\" | grep -qE '\\.swift$'; then swiftformat \"$f\" 2>/dev/null; fi; } || true",
            "timeout": 10,
            "statusMessage": "Running swiftformat..."
          }
        ]
      }
    ]
  }
}
